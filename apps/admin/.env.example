# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com
# Settings → API → Project URL and Publishable Key
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-publishable-key-here

# Note: Publishable key (formerly "anon key") is safe to expose in client-side code.
# It respects Row Level Security (RLS) policies.

# API Configuration
# The app automatically detects the environment and selects the appropriate API URL:
# - Development: Uses http://localhost:3002 (or NEXT_PUBLIC_API_URL if set)
# - Preview/Staging: Uses NEXT_PUBLIC_STAGING_API_URL or NEXT_PUBLIC_API_URL
# - Production: REQUIRED - Must be set in Vercel environment variables
#
# For local development (optional - defaults to http://localhost:3002):
NEXT_PUBLIC_API_URL=http://localhost:3002
#
# For preview/staging deployments (optional):
# NEXT_PUBLIC_STAGING_API_URL=https://api-staging.arreglatodo.com
