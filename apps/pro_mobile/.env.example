# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com
# Settings → API → Project URL and Publishable Key
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-publishable-key-here

# Note: Publishable key (formerly "anon key") is safe to expose in client-side code.
# It respects Row Level Security (RLS) policies.

# API Configuration
# The app automatically detects the environment and selects the appropriate API URL:
# - Development: Uses EXPO_PUBLIC_LOCAL_API_URL or http://localhost:3002
# - Preview: Uses EXPO_PUBLIC_STAGING_API_URL or EXPO_PUBLIC_API_URL
# - Production: REQUIRED - Must be set in eas.json or EAS secrets
#
# For local development with Expo Go (use your local IP for physical devices):
# EXPO_PUBLIC_LOCAL_API_URL=http://192.168.1.100:3002
# Or use EXPO_PUBLIC_API_URL as fallback:
EXPO_PUBLIC_API_URL=http://localhost:3002
#
# For preview/staging builds (set in eas.json or EAS secrets):
# EXPO_PUBLIC_STAGING_API_URL=https://api-staging.arreglatodo.com
#
# For production builds (set in eas.json or EAS secrets):
# EXPO_PUBLIC_API_URL=https://api.arreglatodo.com

# Sentry Configuration
# For local development, only EXPO_PUBLIC_SENTRY_DSN is required
# Get your DSN from Sentry project settings: https://sentry.io/settings/projects/
EXPO_PUBLIC_SENTRY_DSN=https://your-dsn@sentry.io/your-project-id

# For EAS Builds (production) - these are used by the @sentry/react-native/expo plugin
# Set these as EAS secrets: eas secret:create --scope project --name <NAME> --value <VALUE>
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=your-project-name
# SENTRY_AUTH_TOKEN=your-auth-token-here
# SENTRY_URL=https://sentry.io/  # Optional, defaults to https://sentry.io/

# To get SENTRY_AUTH_TOKEN:
# 1. Go to Sentry Settings → Auth Tokens: https://sentry.io/settings/account/api/auth-tokens/
# 2. Create a new token with scopes: project:releases, org:read, project:read, project:write
# 3. Use this token for SENTRY_AUTH_TOKEN
