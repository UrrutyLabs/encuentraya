version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - corepack prepare pnpm@latest --activate
        - cd ../..
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm turbo run build --filter=admin
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .turbo/**/*
